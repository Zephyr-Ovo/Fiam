<?xml version="1.0" encoding="utf-8"?>
<!--
  Favilla accessibility service configuration.

  Subscribes to text-selection-change and a few closely-related events so we
  can remember the user's most recent selection. No feedback / no audio.
  canRetrieveWindowContent allows fallback polling on demand.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewTextSelectionChanged|typeViewTextChanged|typeViewClicked|typeViewLongClicked|typeViewFocused"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="0"
    android:description="@string/a11y_description"
    android:summary="@string/a11y_summary" />
