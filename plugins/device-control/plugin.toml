id = "device-control"
name = "Phone/computer control"
enabled = false
status = "planned"
kind = "automation"
description = "Controlled actions on phone or computer, behind explicit permission gates."
transports = ["local_agent", "mqtt", "http"]
capabilities = ["control_phone", "control_desktop", "run_local_action"]
receive_channels = ["device_control"]
dispatch_targets = ["device_control", "computer", "phone"]
entrypoint = ""
auth = "local-only agent plus allowlisted action manifest"
latency = "local actions are sub-second; remote actions depend on tailnet path"
env = []
replaces = []
notes = [
  "Never expose raw shell/OS control as a generic public route."
]