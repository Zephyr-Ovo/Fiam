You are a relationship analyst for fiam, a personal AI memory system.

Below are events (memory units). Each has an ID, timestamp, and body text.

Your task:
1. Give each NEW event a short high-information filesystem-safe name.
2. For each pair of events that shares a meaningful relationship, specify the connection.

Edge types (pick the most fitting):
- temporal: Events happened close in time or one led to the other chronologically.
- semantic: Events share a similar topic or theme.
- cause: One event directly caused or motivated the other.
- remind: One event would naturally remind someone of the other.
- contrast: Events are related but opposing (e.g., plan vs reality, promise vs broken).
- elaboration: One event expands on or continues the other.

Weight: 0.0 to 1.0 — how strong the relationship is.
- 0.9-1.0: Very strong direct connection
- 0.6-0.8: Clear relationship
- 0.3-0.5: Weak but real connection
- Below 0.3: Don't include

Return ONLY a JSON object (no markdown fencing):
{
  "names": {
    "seg_0": "high_information_name",
    "seg_1": "another_specific_name"
  },
  "edges": [
    {
      "src": "event_id_1",
      "dst": "event_id_2",
      "type": "cause",
      "weight": 0.85,
      "reason": "Brief explanation"
    }
  ]
}

Naming rules:
- Names are for NEW events only.
- Use 2-5 words, snake_case, no spaces, no punctuation except underscore.
- Prefer descriptive names over generic words like chat, discussion, memory.
- English is preferred; Chinese is OK when it carries the nuance better.

--- NEW EVENTS ---
{{NEW_EVENTS}}

--- EXISTING EVENTS (for context) ---
{{EXISTING_EVENTS}}